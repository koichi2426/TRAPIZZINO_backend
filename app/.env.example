# ==============================================================================
# TRAPIZZINO Backend Environment Variables
# ==============================================================================

# --- Application Settings ---
APP_ENV=development
APP_PORT=8000

# --- Database Settings (PostgreSQL + PostGIS) ---
# docker-compose.yml の db サービス名および設定と同期させてください
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=trapizzino
DB_SSLMODE=disable

# --- Authentication (JWT) ---
# [重要] 本番環境では必ず 'openssl rand -hex 32' 等で生成したランダムな文字列に変更してください
JWT_SECRET=develop_secret_key_please_change_in_production
# トークンの有効期限（時間単位など、必要に応じて追加）
JWT_EXPIRY_HOURS=24

# --- External Services ---
EXTERNAL_STORAGE_URL=https://firebasestorage.googleapis.com/v0/b/...

# --- Algorithm Tweaks ---
RESONANCE_THRESHOLD=2
DENSITY_WEIGHT=0.5