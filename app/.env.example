# ==============================================================================
# TRAPIZZINO Backend Environment Variables
# ==============================================================================

# --- Application Settings ---
APP_ENV=development
APP_PORT=8000

# --- Database Settings (PostgreSQL + PostGIS) ---
# docker-compose.yml の db サービス名および設定と同期させてください
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=trapizzino
DB_SSLMODE=disable

# --- Authentication (Infrastructure Domain Impl) ---
# 将来的に AuthDomainService で使用する秘密鍵など
JWT_SECRET=your_super_secret_key_change_me_in_production

# --- External Services (Future Extensions) ---
# 画像アップロード先（Firebase Storage等）を想定
EXTERNAL_STORAGE_URL=https://firebasestorage.googleapis.com/v0/b/...

# --- Algorithm Tweaks ---
# 蒸留アルゴリズムで使用するパラメータの雛形
RESONANCE_THRESHOLD=2
DENSITY_WEIGHT=0.5